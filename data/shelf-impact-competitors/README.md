# Shelf Impact Competitor Inputs

Drop competitor label photos in this folder and run:

```bash
npm run shelf:analyze
```

The analyzer accepts `.jpg`, `.jpeg`, `.png`, `.webp`, and `.avif`. If this folder has no image files, it falls back to the benchmark palettes in `data/shelf-impact-inputs.json`.
